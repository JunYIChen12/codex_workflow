name: Validate Docs

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  validate:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run docs validation
        shell: pwsh
        run: .\scripts\validate-docs.ps1

